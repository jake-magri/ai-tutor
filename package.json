{
  "name": "ai-tutor-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && flask run\" \"cd client && npm run dev\"",
    "install": "concurrently \"cd server && pip install -r requirements.txt\" \"cd client && npm install\"",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
